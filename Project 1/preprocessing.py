def preprocessing(f1, f2, cond="|", addit=""):
    if (len(str(f1))) < 20:
        addit += "ANY"
    if (len(str(f1))) >= 20:
        addit += "ANY"
    if pipe in str(f2):
        addit += "ANY"
    if pipe not in str(f2):
        addit += "ANY"
    return val1

# this file is used for pre-processing the dataset in the future
